
#reattach to user namespace 
  set-option -g default-command "reattach-to-user-namespace -l zsh"
  # bind copy
  bind C-c run "tmux show-buffer | reattach-to-user-namespace pbcopy"
  # bind paste 
  bind C-v run "reattach-to-user-namespace pbpaste | tmux load-buffer - && tmux paste-buffer"


set -g default-terminal "screen-256color"

#replace action key as C-a 
set-option -g prefix C-a

#use vim keybindings 
setw -g mode-keys vi 

#nav keys 
bind h select-pane -L 
bind j select-pane -D 
bind k select-pane -U 
bind l select-pane -R 

#resize pnes
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5


#split pane keys
bind s split-window -v
bind v split-window -h


#reload 
bind r source-file ~/.tmux.conf

#kill the session 
bind R kill-session

#start index with 1. very un-dev
set -g base-index 1
setw -g pane-base-index 1


# ***************
# theme 
# *************** 

# # Coloring everything
set -g status-fg white
set -g status-bg black
setw -g window-status-fg blue
setw -g window-status-bg default
setw -g window-status-attr dim
setw -g window-status-current-fg black
setw -g window-status-current-bg blue
#setw -g window-status-current-attr bright
set -g pane-border-fg white
set -g pane-border-bg black
set -g pane-active-border-fg cyan
set -g pane-active-border-bg black
set -g message-fg white
set -g message-bg black
set -g message-attr bright

# statusbar
set -g status-utf8 on
set -g status-interval 60
set -g status-left-length 40
set -g status-justify centre
set -g status-left "#[fg=blue]#(git rev-parse --abbrev-ref HEAD)"
set -g status-right "#[fg=blue]%R #(~/bin/battery)"


#status bar 
#set -g status-bg black 
#set -g status-fg white 
#set -g status-left '#[fg=cyan]#(git rev-parse --abbrev-ref HEAD)'

#highlight 
#set-window-option -g window-status-current-bg blue 
#set-window-option -g window-status-current-fg black 


